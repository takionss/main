// Translation Dictionary
const TRANSLATIONS = {
    ko: {
        title: "이미지 GIF 변환기 (고화질 움짤 만들기) | SmilesLife",
        meta_desc: "여러 장의 사진을 합쳐 간편하고 빠르게 고화질 움직이는 GIF(움짤) 파일을 제작해주는 도구입니다. 프레임 순서 드래그 정렬 및 딜레이 슬라이더 조절 지원.",
        header_title: "이미지 GIF 변환기",
        header_desc: "여러 장의 이미지를 합쳐 나만의 움짤을 만들어보세요.",
        label_settings: "설정",
        label_speed: "재생 속도 (프레임 지연)",
        label_size: "출력 가로 크기 (픽셀)",
        option_500: "500 px (기본)",
        option_original: "원본 크기",
        btn_generate: "GIF 만들기",
        title_preview: "실시간 미리보기",
        no_preview_desc: "이미지를 추가하면<br>미리보기가 표시됩니다.",
        title_timeline: "프레임 타임라인",
        btn_add_image: "이미지 추가",
        btn_clear_all: "전체 삭제",
        empty_timeline_title: "이미지를 업로드하여 프레임을 구성하세요",
        empty_timeline_desc: "드래그하여 순서를 바꿀 수 있습니다.",
        modal_generating_title: "GIF 인코딩 중...",
        modal_generating_desc: "고품질 움짤을 만들고 있습니다.",
        modal_finished_title: "GIF 생성 완료!",
        btn_download: "다운로드",
        btn_close: "닫기",
        footer_text: "© 2026 AI Utility Web. 모든 작업은 브라우저 로컬에서 안전하게 처리됩니다.",
        confirm_clear_all: "모든 이미지를 삭제하시겠습니까?",
        alert_min_frames: "최소 2개 이상의 이미지가 필요합니다.",
        alert_error: "오류 발생: "
    },
    en: {
        title: "Image to GIF Converter (Make High-Quality GIFs) | SmilesLife",
        meta_desc: "A tool to quickly and easily make high-quality moving GIF files by combining multiple photos. Supports frame drag-and-drop sorting and delay slider control.",
        header_title: "Image to GIF Converter",
        header_desc: "Combine multiple images to create your own animated GIF.",
        label_settings: "Settings",
        label_speed: "Playback Speed (Frame Delay)",
        label_size: "Output Width (pixels)",
        option_500: "500 px (Default)",
        option_original: "Original Size",
        btn_generate: "Create GIF",
        title_preview: "Real-time Preview",
        no_preview_desc: "Add images to see<br>the preview here.",
        title_timeline: "Frame Timeline",
        btn_add_image: "Add Images",
        btn_clear_all: "Clear All",
        empty_timeline_title: "Upload images to build your frames",
        empty_timeline_desc: "You can drag and drop to change the order.",
        modal_generating_title: "Encoding GIF...",
        modal_generating_desc: "Creating a high-quality GIF.",
        modal_finished_title: "GIF Generation Complete!",
        btn_download: "Download",
        btn_close: "Close",
        footer_text: "© 2026 AI Utility Web. All tasks are processed safely in your browser locally.",
        confirm_clear_all: "Are you sure you want to delete all images?",
        alert_min_frames: "At least 2 images are required.",
        alert_error: "An error occurred: "
    },
    es: {
        title: "Convertidor de Imagen a GIF (Crear GIFs de Alta Calidad) | SmilesLife",
        meta_desc: "Una herramienta para crear rápida y fácilmente archivos GIF en movimiento de alta calidad combinando varias fotos. Admite ordenación de fotogramas por arrastrar y soltar y control del control deslizante de retardo.",
        header_title: "Convertidor de Imagen a GIF",
        header_desc: "Combina varias imágenes para crear tu propio GIF animado.",
        label_settings: "Configuración",
        label_speed: "Velocidad de reproducción (retraso de fotograma)",
        label_size: "Ancho de salida (píxeles)",
        option_500: "500 px (Predeterminado)",
        option_original: "Tamaño original",
        btn_generate: "Crear GIF",
        title_preview: "Vista previa en tiempo real",
        no_preview_desc: "Añade imágenes para ver<br>la vista previa aquí.",
        title_timeline: "Línea de tiempo de fotogramas",
        btn_add_image: "Añadir imágenes",
        btn_clear_all: "Borrar todo",
        empty_timeline_title: "Sube imágenes para construir tus fotogramas",
        empty_timeline_desc: "Puedes arrastrar y soltar para cambiar el orden.",
        modal_generating_title: "Codificando GIF...",
        modal_generating_desc: "Creando un GIF de alta calidad.",
        modal_finished_title: "¡Creación de GIF completa!",
        btn_download: "Descargar",
        btn_close: "Cerrar",
        footer_text: "© 2026 AI Utility Web. Todas las tareas se procesan de forma segura localmente en su navegador.",
        confirm_clear_all: "¿Estás seguro de que quieres eliminar todas las imágenes?",
        alert_min_frames: "Se requieren al menos 2 imágenes.",
        alert_error: "Ocurrió un error: "
    },
    ja: {
        title: "画像からGIFへの変換 (高品質なGIFアニメ作成) | SmilesLife",
        meta_desc: "複数の写真を合成して簡単かつ迅速に高品質なGIF画像を作成するツールです。ドラッグ＆ドロップによるフレームの並び替えと遅延スライダー調整に対応。",
        header_title: "画像 GIF 変換器",
        header_desc: "複数の画像を組み合わせて、自分だけのオリジナルGIFアニメーションを作ってみましょう。",
        label_settings: "設定",
        label_speed: "再生速度 (フレーム遅延)",
        label_size: "出力の幅 (ピクセル)",
        option_500: "500 px (デフォルト)",
        option_original: "元のサイズ",
        btn_generate: "GIFを作成する",
        title_preview: "リアルタイムプレビュー",
        no_preview_desc: "画像を追加すると<br>プレビューが表示されます。",
        title_timeline: "フレームタイムライン",
        btn_add_image: "画像を追加",
        btn_clear_all: "すべて削除",
        empty_timeline_title: "画像をアップロードしてフレームを構築します",
        empty_timeline_desc: "ドラッグで順序を入れ替えることができます。",
        modal_generating_title: "GIFエンコード中...",
        modal_generating_desc: "高品質なGIFを作成しています。",
        modal_finished_title: "GIF作成完了！",
        btn_download: "ダウンロード",
        btn_close: "閉じる",
        footer_text: "© 2026 AI Utility Web. すべての処理はブラウザ内で安全に実行されます。",
        confirm_clear_all: "すべての画像を削除してもよろしいですか？",
        alert_min_frames: "最低2枚以上の画像が必要です。",
        alert_error: "エラーが発生しました: "
    },
    'zh-TW': {
        title: "圖片轉 GIF 產生器 (製作高畫質動圖) | SmilesLife",
        meta_desc: "快速輕鬆地將多張照片組合成高品質的動態 GIF 圖片。支援拖放排序影格與延遲滑桿調整功能。",
        header_title: "圖片 GIF 轉換器",
        header_desc: "合併多張圖片，製作屬於您自己的動態 GIF。",
        label_settings: "設定",
        label_speed: "播放速度 (影格延遲)",
        label_size: "輸出寬度 (像素)",
        option_500: "500 px (預設)",
        option_original: "原始大小",
        btn_generate: "製作 GIF",
        title_preview: "即時預覽",
        no_preview_desc: "新增圖片後<br>即可在此顯示預覽。",
        title_timeline: "影格時間軸",
        btn_add_image: "新增圖片",
        btn_clear_all: "全部刪除",
        empty_timeline_title: "上傳圖片以建立影格時間軸",
        empty_timeline_desc: "您可以拖曳圖片來調整順序。",
        modal_generating_title: "GIF 編碼中...",
        modal_generating_desc: "正在製作高品質的 GIF...",
        modal_finished_title: "GIF 製作完成！",
        btn_download: "下載",
        btn_close: "關閉",
        footer_text: "© 2026 AI Utility Web. 所有作業均在瀏覽器本機端安全處理。",
        confirm_clear_all: "確定要刪除所有圖片嗎？",
        alert_min_frames: "請至少新增 2 張圖片。",
        alert_error: "發生錯誤: "
    }
};

window.l10n = {
    currentLang: 'ko',
    t: function(key) {
        return TRANSLATIONS[this.currentLang]?.[key] || TRANSLATIONS['en']?.[key] || key;
    }
};

function initLanguage() {
    const urlParams = new URLSearchParams(window.location.search);
    let lang = urlParams.get('lang');
    if (!lang) {
        lang = localStorage.getItem('smileslife-preferred-lang');
    }
    if (!lang && navigator.language) {
        const navLang = navigator.language.toLowerCase();
        if (navLang.startsWith('ko')) lang = 'ko';
        else if (navLang.startsWith('es')) lang = 'es';
        else if (navLang.startsWith('ja')) lang = 'ja';
        else if (navLang.startsWith('zh-tw') || navLang.startsWith('zh-hk')) lang = 'zh-TW';
        else lang = 'en';
    }
    if (!['ko', 'en', 'es', 'ja', 'zh-TW'].includes(lang)) {
        lang = 'ko';
    }
    
    const langSelect = document.getElementById('lang-select');
    if (langSelect) langSelect.value = lang;
    
    applyLanguage(lang);
}

function applyLanguage(lang) {
    window.l10n.currentLang = lang;
    document.documentElement.lang = lang;
    localStorage.setItem('smileslife-preferred-lang', lang);
    
    document.querySelectorAll('[data-i18n]').forEach(el => {
        const key = el.getAttribute('data-i18n');
        const text = TRANSLATIONS[lang]?.[key] || TRANSLATIONS['en']?.[key];
        if (text) {
            if (el.tagName === 'TITLE') {
                document.title = text;
            } else if (el.innerHTML.includes('<br>') || text.includes('<br>')) {
                el.innerHTML = text;
            } else {
                el.textContent = text;
            }
        }
    });
}

// Application State
let frames = []; // Array of { id, dataUrl, file }
let currentPreviewIndex = 0;
let previewInterval = null;
let isGenerating = false;

// DOM Elements
const fileInput = document.getElementById('file-input');
const timelineGrid = document.getElementById('timeline-grid');
const emptyState = document.getElementById('empty-state');
const delaySlider = document.getElementById('delay-slider');
const delayValText = document.getElementById('delay-val');
const sizeSelect = document.getElementById('size-select');
const generateBtn = document.getElementById('generate-btn');
const clearAllBtn = document.getElementById('clear-all');
const previewCanvas = document.getElementById('preview-canvas');
const noPreviewOverlay = document.getElementById('no-preview');

const resultModal = document.getElementById('result-modal');
const generatingState = document.getElementById('generating-state');
const finishedState = document.getElementById('finished-state');
const renderProgressBar = document.getElementById('render-progress');
const progressText = document.getElementById('progress-text');
const resultGifImg = document.getElementById('result-gif');
const downloadBtn = document.getElementById('download-btn');

let resultBlob = null;

// Initialize SortableJS
const sortable = new Sortable(timelineGrid, {
    animation: 150,
    ghostClass: 'sortable-ghost',
    onEnd: () => {
        const newOrderIds = Array.from(timelineGrid.querySelectorAll('.frame-item')).map(el => el.dataset.id);
        const reorderedFrames = newOrderIds.map(id => frames.find(f => f.id === id));
        frames = reorderedFrames;
        resetPreview();
    }
});

// Global Drag & Drop Prevention
['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
    window.addEventListener(eventName, preventDefaults, false);
});

function preventDefaults(e) {
    e.preventDefault();
    e.stopPropagation();
}

const dropZone = document.getElementById('drop-zone');
['dragenter', 'dragover'].forEach(eventName => {
    dropZone.addEventListener(eventName, () => dropZone.classList.add('bg-indigo-50/30', 'border-2', 'border-dashed', 'border-indigo-400'), false);
});
['dragleave', 'drop'].forEach(eventName => {
    dropZone.addEventListener(eventName, () => dropZone.classList.remove('bg-indigo-50/30', 'border-2', 'border-dashed', 'border-indigo-400'), false);
});
dropZone.addEventListener('drop', (e) => {
    handleFileUpload({ target: { files: e.dataTransfer.files } });
});

fileInput.addEventListener('change', handleFileUpload);
delaySlider.addEventListener('input', (e) => {
    delayValText.textContent = `${e.target.value}ms`;
    resetPreview();
});
sizeSelect.addEventListener('change', resetPreview);
generateBtn.addEventListener('click', generateGif);
clearAllBtn.addEventListener('click', clearAllFrames);

function handleFileUpload(e) {
    const files = Array.from(e.target.files);
    if (files.length === 0) return;
    files.forEach(file => {
        if (!file.type.startsWith('image/')) return;
        const reader = new FileReader();
        reader.onload = (event) => {
            frames.push({ id: Date.now() + Math.random().toString(36).substr(2, 9), dataUrl: event.target.result, file: file });
            renderTimeline();
            if (frames.length === 1) startPreview();
        };
        reader.readAsDataURL(file);
    });
    fileInput.value = '';
}

function renderTimeline() {
    if (frames.length === 0) {
        emptyState.classList.remove('hidden');
        timelineGrid.innerHTML = '';
        generateBtn.disabled = true;
        stopPreview();
        return;
    }
    emptyState.classList.add('hidden');
    generateBtn.disabled = false;
    timelineGrid.innerHTML = '';
    frames.forEach(frame => {
        const div = document.createElement('div');
        div.className = 'frame-item bg-slate-100 rounded-xl overflow-hidden aspect-square border border-slate-200 shadow-sm';
        div.dataset.id = frame.id;
        div.innerHTML = `<img src="${frame.dataUrl}" class="w-full h-full object-cover"><button class="delete-btn absolute top-1 right-1 bg-red-500 text-white w-6 h-6 rounded-lg flex items-center justify-center text-xs shadow-md hover:bg-red-600 transition-colors" onclick="deleteFrame('${frame.id}')"><i class="fas fa-times"></i></button>`;
        timelineGrid.appendChild(div);
    });
}

window.deleteFrame = function(id) {
    frames = frames.filter(f => f.id !== id);
    renderTimeline();
    resetPreview();
};

function clearAllFrames() {
    if (confirm(window.l10n.t('confirm_clear_all'))) { frames = []; renderTimeline(); resetPreview(); }
}

function startPreview() {
    if (previewInterval) clearInterval(previewInterval);
    noPreviewOverlay.classList.add('hidden');
    function step() {
        if (frames.length === 0) { stopPreview(); return; }
        drawPreviewFrame(frames[currentPreviewIndex]);
        currentPreviewIndex = (currentPreviewIndex + 1) % frames.length;
    }
    step();
    previewInterval = setInterval(step, parseInt(delaySlider.value));
}

function stopPreview() {
    if (previewInterval) clearInterval(previewInterval);
    previewInterval = null;
    noPreviewOverlay.classList.remove('hidden');
    previewCanvas.getContext('2d').clearRect(0, 0, previewCanvas.width, previewCanvas.height);
}

function resetPreview() { currentPreviewIndex = 0; if (frames.length > 0) startPreview(); else stopPreview(); }

function drawPreviewFrame(frame) {
    const img = new Image();
    img.onload = () => {
        const sizeMode = sizeSelect.value;
        const targetWidth = sizeMode === 'original' ? img.width : parseInt(sizeMode);
        const targetHeight = (img.height / img.width) * targetWidth;
        previewCanvas.width = targetWidth;
        previewCanvas.height = targetHeight;
        previewCanvas.getContext('2d').drawImage(img, 0, 0, targetWidth, targetHeight);
    };
    img.src = frame.dataUrl;
}

// THE ULTIMATE GIF FIX: USE IMAGE ELEMENT DIRECTLY
async function generateGif() {
    if (frames.length < 2) { alert(window.l10n.t('alert_min_frames')); return; }
    isGenerating = true;
    showModal();
    updateProgress(5);

    let workerUrl = 'https://cdn.jsdelivr.net/npm/gif.js@0.2.0/dist/gif.worker.js';
    try {
        const response = await fetch(workerUrl);
        workerUrl = URL.createObjectURL(new Blob([await response.text()], { type: 'application/javascript' }));
    } catch (e) { console.error("Worker blob failed"); }

    const gif = new GIF({ workers: 2, quality: 10, workerScript: workerUrl });

    try {
        for (let i = 0; i < frames.length; i++) {
            const img = await loadImage(frames[i].dataUrl);
            const sizeMode = sizeSelect.value;
            const tw = Math.floor(sizeMode === 'original' ? img.width : parseInt(sizeMode));
            const th = Math.floor((img.height / img.width) * tw);
            
            // Create a dedicated canvas for each frame to avoid type issues
            const fCanvas = document.createElement('canvas');
            fCanvas.width = tw;
            fCanvas.height = th;
            fCanvas.getContext('2d').drawImage(img, 0, 0, tw, th);
            
            // Pass the CANVAS element directly. gif.js likes this better than context.
            gif.addFrame(fCanvas, { copy: true, delay: parseInt(delaySlider.value) });
            updateProgress(5 + Math.round(((i + 1) / frames.length) * 20));
        }

        gif.on('progress', (p) => updateProgress(25 + Math.round(p * 75)));
        gif.on('finished', (blob) => {
            updateProgress(100);
            resultBlob = blob;
            resultGifImg.src = URL.createObjectURL(blob);
            generatingState.classList.add('hidden');
            finishedState.classList.remove('hidden');
            isGenerating = false;
        });
        gif.render();
    } catch (err) {
        alert(window.l10n.t('alert_error') + err.message);
        closeModal();
    }
}

function loadImage(src) { return new Promise((res, rej) => { const img = new Image(); img.onload = () => res(img); img.onerror = rej; img.src = src; }); }
function showModal() { resultModal.classList.remove('hidden'); generatingState.classList.remove('hidden'); finishedState.classList.add('hidden'); updateProgress(0); }
window.closeModal = function() { if (!isGenerating) resultModal.classList.add('hidden'); }
function updateProgress(p) { renderProgressBar.style.width = `${p}%`; progressText.textContent = `${p}%`; }
downloadBtn.addEventListener('click', () => {
    if (!resultBlob) return;
    const a = document.createElement('a');
    a.href = URL.createObjectURL(resultBlob);
    a.download = `smileslife_animation.gif`;
    a.click();
});

// Init language selector event listener and run initial check
document.addEventListener('DOMContentLoaded', () => {
    const langSelect = document.getElementById('lang-select');
    if (langSelect) {
        langSelect.addEventListener('change', (e) => {
            applyLanguage(e.target.value);
        });
    }
    initLanguage();
});